// A script to populate the database with the json data from categories.json
// This script also creates a different TopicCollection (schema) for each category, holding the survey id of each survey (or topic) in that category
